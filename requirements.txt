# Evidentia — MSL Intelligence Platform
# Direct runtime dependencies only.
#
# This replaces a 101-line full `pip freeze` whose exact 2026-dated pins had no
# matching wheels on Streamlit Cloud's Python, which aborted the whole install
# and left langgraph (and everything else) uninstalled -> ModuleNotFoundError.
# Floors + major caps let pip resolve a compatible set on whatever Python the
# host runs, while keeping the APIs the code targets (langgraph 1.x, pydantic 2.x).
# Transitive deps (anthropic, openai, numpy, altair, langgraph-checkpoint, ...)
# are pulled automatically — do not pin them here.

# Web app
streamlit>=1.40,<2

# Agent orchestration + LLM
langgraph>=1.0,<2
langchain-core>=1.0,<2
langchain-anthropic>=1.0,<2
langchain-openai>=1.0,<2

# State / settings schema
pydantic>=2.7,<3
pydantic-settings>=2.0,<3

# Evidence + data sources
tavily-python>=0.5
biopython>=1.83
requests>=2.31

# Output / export
reportlab>=4.0

# Data + charts
pandas>=2.0
plotly>=5.20

# Resilience
tenacity>=8.0
